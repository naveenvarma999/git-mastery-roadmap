# Day 13 - Git Commit Amend

## What is amend?

`git commit --amend` replaces the latest commit with a corrected version.

## Change latest commit message

```bash
git commit --amend -m "Correct commit message"